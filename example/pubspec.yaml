name: flutter_ai_anthropic_llm_provider_example
description: Example app for flutter_ai_anthropic_llm_provider
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  flutter_ai_anthropic_llm_provider:
    path: ../
  
  flutter_ai_toolkit: ^0.6.8
  cupertino_icons: ^1.0.2
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
